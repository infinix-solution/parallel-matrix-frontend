# Parallel Matrix — Frontend (Angular 16 Standalone)

## Run
```bash
npm install
npm start            # http://localhost:4200
```

Make sure the backend is running on http://localhost:3000.

## Admin
Go to http://localhost:4200/admin/login
- Email:    admin@parallelmatrixcorp.com
- Password: Admin@PM2026

## Drop assets in `src/assets/`
See `src/assets/README.md` for the list.
